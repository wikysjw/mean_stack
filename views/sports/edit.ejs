<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <% include ../partials/head %>
</head>
<body>
        <% include ../partials/nav %>

        <div class="container post post-edit">
            <% include ./partials/header %>

            <div class="buttons">
                <a class="btn btn-default" href="/sports">뒤로가기</a>
            </div>

            <form class="post-form form-horizontal" action="/sports/<%= sport._id %>?_method=put" method="post">
                <div class="contentBox">
                    <h3 class="contentBoxTop">게시물 수정</h3>
                    <fieldset>
                        <div class="form-group">
                            <label for="title" class="col-sm-2 control-label">Title</label>
                            <div class="col-sm-10">
                                <input class="form-control" type="text" id="title" name="title" value="<%= sport.title %>">
                            </div>
                        </div>
                        <div class="form-group">
                            <label for="body" class="col-sm-2 control-label">Body</label>
                            <div class="col-sm-10">
                                <textarea class="form-control" id="body" name="body" rows="5"><%= sport.body %></textarea>
                            </div>
                        </div>
                    </fieldset>
                </div>
                <div class="buttons">
                    <button type="submit" class="btn btn-default">확인</button>
                </div>
            </form>
            
        </div>
</body>
</html>